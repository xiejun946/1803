数据库不等于实例，实例是用来管理数据库的
多个数据库可以通过多个实例来管理连接
分布式处理的安装，必须安装oracle net组件
oracle 在数据一致性方便可以避免脏读，在A修改时，B无法读取会报错，A修改保存过后才能正确读取
表是存储数据的对象，
视图是用来给不同用户显示不同数据，类似权限
同义词是一个指向真正表的指针
存储对象就是存储过程，一类代码段
oracle启动需要首先打开监听，其次，进入sqlplusstartup
环境换辆没配置好，就可能导致无法开启监听，或者中文乱码，无法启动的问题
lsnrctl start

export PATH
export ORACLE_HOME=/u01/app/oracle/product/12.2.0/dbhome_1/
export PATH=$ORACLE_HOME/bin:$PATH
export ORACLE_SID=orcl

问题1 ，乱码问题
